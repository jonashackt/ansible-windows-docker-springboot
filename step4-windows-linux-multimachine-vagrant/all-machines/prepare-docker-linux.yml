---
- hosts: "{{host}}"
  become: yes
  gather_facts: true

  pre_tasks:
    - name: Update apt cache if needed.
      apt: update_cache=yes cache_valid_time=3600

  tasks:
    - include: install-docker-linux.yml